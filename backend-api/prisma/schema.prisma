generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Feature {
  id          Int       @id
  name        String
  description String?
  createdAt   DateTime
  deletedAt   DateTime?
}

